# 自律小窝 · 安卓桌面自律小程序（PWA）

一款温馨治愈风的桌面自律小程序，含 **每日新闻 / 政协工作干货 / 公文写作学习** 三大板块。
纯静态网页（PWA），可像 App 一样「添加到主屏幕」，离线也能看。

---

## 一、在电脑上预览（开发/检查用）

进入本文件夹，启动一个本地服务（必须用 http，不能用 `file://`，否则 PWA 不生效）：

```bash
python -m http.server 8080
```

然后用浏览器打开 `http://localhost:8080` 即可查看效果。

---

## 二、安装到安卓手机桌面（重点）

### 方式 A：用 Chrome 浏览器（推荐）
1. 把本文件夹部署到一个**可访问的网址**（见下方「部署」）。
2. 安卓手机用 **Chrome** 打开该网址。
3. 点击右上角 **⋮ 菜单 →「添加到主屏幕」**（或地址栏右侧的「安装」图标 📥）。
4. 命名「自律小窝」，确认后桌面即出现图标，点击即可全屏独立运行。

### 方式 B：用 Edge 浏览器
1. 手机 Edge 打开网址 → 点击 **⋮ →「添加到主屏幕」**。
2. 后续用法同 Chrome。

> 提示：部分国产安卓系统（如华为、小米）的默认浏览器对 PWA 支持不一致，
> 建议直接用 **Chrome / Edge** 操作最稳妥。若菜单没有「添加到主屏幕」，
> 可先「收藏」再在收藏夹里选择「添加到桌面」。

---

## 三、免费部署（让手机能装）

静态文件可直接托管到以下任意免费服务（无需服务器、无需备案）：

- **Cloudflare Pages**：连接仓库或拖拽上传本文件夹，自动生成 https 网址。
- **GitHub Pages**：把本文件夹推到仓库，开启 Pages 即可。
- **Vercel / Netlify**：拖拽上传，秒级生成网址。

拿到 https 网址后，用手机浏览器打开并按「二」安装即可。

---

## 四、目录结构

```
index.html            应用外壳
styles.css            治愈清新视觉
app.js                渲染与交互
data.js               内置内容（新闻 / 政协干货 / 公文写作）
manifest.webmanifest  PWA 清单（名称、图标、standalone）
sw.js                 离线缓存服务工作线程
icons/                桌面图标（192 / 512）
使用说明.md           本文件
```

---

## 五、如何更新内容

三大板块内容由 `data.js` 内置（带 `updatedDate` 日期）。
- **想刷新内容时**：让 AI 重新联网抓取并替换 `data.js` 即可，无需改动其他文件。
- 新增条目：往对应数组追加 `{id, title, source, summary, points[], url}` 对象。
- 改每日寄语：编辑 `mottos` 数组。

---

## 六、小提示

- 离线可用：安装后断网也能查看已加载过的内容（service worker 缓存）。
- 每天早晨打开看一眼「每日新闻」与两条干货，利用碎片时间学习。
- 内容来源以主流媒体、政协写作方法论与公文写作技能为准，详情可点击「前往来源查看原文」。
